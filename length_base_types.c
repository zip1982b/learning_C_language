#include <stdio.h>

int main(){
	printf("Hello World\n");
    printf("тип int = %ld байтов, в данной машине\n", sizeof(int));
    printf("тип float = %ld байтов, в данной машине\n", sizeof(float));
    printf("тип char = %ld байтов, в данной машине\n", sizeof(char));

    return 0;
}

